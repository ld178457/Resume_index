# ============================================================
# 本地开发 / 编辑器
# ============================================================
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# ============================================================
# 项目内部文件 —— 严禁提交
# .workbuddy/ 里存放项目记忆、交付文档、账号链接原文等，
# 其中含手机号、邮箱、FTP 凭据等敏感信息，一旦推到公开仓库即泄露
# ============================================================
.workbuddy/
SVG.txt
svg.txt
各账号链接.txt

# 依赖
node_modules/
